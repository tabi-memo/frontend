query activityCollection($uuid: UUID!) {
  activityCollection(filter: { uuid: { eq: $uuid } }) {
    edges {
      node {
        id
        uuid
        trip_id
        title
        time_from
        time_to
        address
        url
        memo
        cost
        image_storage_object_id
      }
    }
  }
}
